import x from 'y';

const Search = () => {
	return (
		<section className={}>
			<form className={}>
				<div className={}>
					<label htmlFor=""></label>
					<input type="text"/>
				</div>
			</form>
		</section>
	);
};